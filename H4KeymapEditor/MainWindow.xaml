<Window x:Class="H4KeymapEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:H4KeymapEditor"
        xmlns:vm="clr-namespace:H4KeymapEditor.ViewModels"
        mc:Ignorable="d"
        Title="Halo 4 Keymapping Editor" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <CollectionViewSource x:Key="KeycodesSource" Source="{Binding Keycodes}" />
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Open"
                    Executed="Open_Executed"/>

        <CommandBinding Command="ApplicationCommands.Save"
                    Executed="Save_Executed"/>
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Menu Grid.Row="0" Grid.Column="0">
            <MenuItem Header="File">
                <MenuItem Header="_Open" Command="ApplicationCommands.Open" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Save" Command="ApplicationCommands.Save" InputGestureText="Ctrl+S"/>
            </MenuItem>
            <MenuItem Header="Preferences">
                <MenuItem Header="Use Dark Mode" IsCheckable="True" IsChecked="{Binding UseDarkMode}"/>
                <MenuItem Header="Show Unknowns" IsCheckable="True" IsChecked="{Binding ShowUnknowns}"/>
            </MenuItem>
        </Menu>
        <ComboBox Grid.Row="1" Grid.Column="0" SelectedIndex="0" Visibility="Collapsed">
            <ComboBoxItem Content="Halo 4"/>
        </ComboBox>
        <ComboBox Template="{DynamicResource ComboBoxTemplate}" Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Executables}" SelectedItem="{Binding SelectedExecutable, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="120" Visibility="Collapsed"/>
        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4">
            <DataGrid ItemsSource="{Binding VisibleKeyBindings}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="200"/>
                    <!-- <DataGridTextColumn Header="Offset" Binding="{Binding FileOffset, StringFormat=0x{0:X}}" IsReadOnly="True" Width="100"/> -->
                    <DataGridComboBoxColumn Header="Primary Binding" SelectedItemBinding="{Binding PrimaryKey, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Source={StaticResource KeycodesSource}}" ElementStyle="{StaticResource DataGridComboBoxStyle}" EditingElementStyle="{StaticResource DataGridComboBoxStyle}" MinWidth="150"/>
                    <!-- <DataGridComboBoxColumn Header="Secondary Binding" SelectedItemBinding="{Binding SecondaryKey, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Source={StaticResource KeycodesSource}}" ElementStyle="{StaticResource DataGridComboBoxStyle}" EditingElementStyle="{StaticResource DataGridComboBoxStyle}" MinWidth="150"/> -->
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

    </Grid>
</Window>
